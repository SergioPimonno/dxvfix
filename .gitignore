# build output
/out/
/out-keygen/
/dist/
*.jar
!/lib/*.jar

# license signing key -- NEVER commit this, it's the master secret for the whole license system
/license-keys/

# website/ is its own separate git repo now (SergioPimonno/dxvfix-website, private) -- excluded
# here so it can never get swept into this repo by an incautious `git add -A`.
/website/

# local tooling config
/.claude/

# large local test assets (not part of the app)
*.mov
*.mp4

# OS/editor cruft
Thumbs.db
.DS_Store
